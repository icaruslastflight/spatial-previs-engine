# Optional browser-facing basemap credentials. Core local projects need neither.
# VITE_ variables are public in the built browser bundle; restrict them appropriately.
VITE_GOOGLE_MAPS_API_KEY=
VITE_CESIUM_ION_TOKEN=
# Never put GDTF passwords or provider private/admin API keys in VITE_ variables.
