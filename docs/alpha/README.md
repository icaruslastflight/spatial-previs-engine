# Alpha Phase (R1)

This directory contains verification and sign-off records for the **Alpha build (R1 phase)** of the Spatial Previs Engine.
We have moved past some earlier R0 approvals and marked them as WIP to expedite testing in this phase.

## Current Status
- R0 checks have been snapshot.
- Content Mapping UI has been implemented in the Map workspace.
- The project is now tracked as `1.0.0-alpha`.
